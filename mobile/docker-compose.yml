version: '3.1'
services:
  mobile-wordpress:
    image: wordpress
    ports:
      - 8084:80
    depends_on:
      - mobile-db
    networks:
      mobile-db:
      web-bus:
    volumes:
      - ${USC_MOBILE_DATA_DIR}:/var/www/html
    deploy:
      replicas: 1
      update_config:
        parallelism: 1
        delay: 10s
      restart_policy:
        condition: on-failure
    environment:
      - WORDPRESS_DB_PASSWORD=${USC_MOBILE_DB_PASSWORD}
      - WORDPRESS_DB_HOST=mobile-db
      - ServerName=${USC_MOBILE_SERVER_NAME}
  mobile-db:
    image: mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${USC_MOBILE_DB_PASSWORD}
    networks:
      mobile-db:
    volumes:
      - ${USC_MOBILE_DB_DIR}:/var/lib/mysql
networks:
  mobile-db:
    driver: overlay
  web-bus:
    external:
      name: web-bus
